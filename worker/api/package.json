{
  "name": "barbarian-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "migrate:local": "wrangler d1 migrations apply barbarian --local"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.0",
    "vitest": "~3.2.0",
    "wrangler": "^4.124.0"
  }
}
